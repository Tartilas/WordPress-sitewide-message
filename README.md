# WordPress sitewide message
> WordPress sitewide message is a plugin that allows administrators to display a list of site authors. 

It also allows some additional options:

- Change background color
- Change text color
- Change URL location
- Schedule message based on your requirements